# Password Manager
A GUI password manager application to keep your all your passwords safe without transferring them over the internet.

# Features
1. Save your passwords to a file called "data.json".
2. Generates strong password.
3. Add your password to the clipboard.
4. Displays all the data file's entries in a json format through notepad (windows).
